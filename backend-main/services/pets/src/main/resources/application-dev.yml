spring:
  jpa:
    properties:
      hibernate:
        format_sql: true
        show_sql: false

logging:
  level:
    org.springframework.security: debug
    org.springframework.web: debug
    dev.pet.pets: debug
